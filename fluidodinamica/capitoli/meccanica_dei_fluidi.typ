

= Meccanica dei fluidi

== Velocità angolare

$
  omega = 1/2 ((dif alpha)/(dif t) + (dif beta)/(dif t)) = 1/2 ((partial u_y)/(partial x) - (partial u_y)/(partial x))
$

== Derivata totale o materiale
Per una grandezza generica b nella direzione generica x:
$
  dif/(dif t) b_x (t,x(t), y(t), z(t)) = (partial b_x)/(partial t) (dif t)/(dif t)
  + (partial b_x)/(partial x) (dif x)/(dif t)
  + (partial b_x)/(partial y) (dif y)/(dif t)
  + (partial b_x)/(partial z) (dif z)/(dif t)
$

Che diventa:
$
  dif/(dif t) b_x (t,x(t), y(t), z(t)) = (partial b_x)/(partial t)
  + (partial b_x)/(partial x) u_x
  + (partial b_x)/(partial y) u_y
  + (partial b_x)/(partial z) u_z
$

Forma compatta per la grandezza $arrow(b)$ vettoriale:
$
  (dif arrow(b))/ (dif t) = 
$